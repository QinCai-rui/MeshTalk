{
  "name": "meshtalk-cli",
  "version": "0.28.3",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --target=bun --compile --outfile dist/meshtalk-cli",
    "test": "bun test"
  },
  "dependencies": {},
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^7.0.2"
  }
}
